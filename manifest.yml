---
applications:
- name: fastly-todo
  memory: 1024M
  instances: 1
  command: bundle exec rake cf:on_first_instance db:migrate && bundle exec rails s -p $PORT -e $RAILS_ENV
  host: fastly-todo
  domains:
  - cfapps.io
  - caseywest.com
  services:
  - fastly-todo-pg
